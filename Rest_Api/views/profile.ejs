<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
</head>
<body>
    <h1>User Profile</h1>
    <% if (result && result.length > 0) { %>
        <p>User ID: <%= result[0].UID %></p>
        <p>Name: <%= result[0].UName %></p>
        <p>Email: <%= result[0].UEmail %></p>
        <p>Address 1: <%= result[0].PAdd1 %></p>
        <p>Address 2: <%= result[0].PAdd2 %></p>
        <p>Pincode: <%= result[0].Pincode %></p>
        <p>Contact: <%= result[0].Contact %></p>
        <!-- You can add more fields as needed -->
    <% } else { %>
        <p>No user data found</p>
    <% } %>
</body>
</html>
